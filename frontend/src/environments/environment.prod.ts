// Production environment configuration
export const environment = {
  production: true,
  apiUrl: 'https://api.hrms-lite.com',
  apiEndpoints: {
    employees: '/api/employees/',
    attendance: '/api/attendance/'
  }
};
